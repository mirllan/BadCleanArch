using System;

namespace WebApi.Controllers
{
    // esta clase es estatica porque asi es mas simple y no genera alertas en sonar
    public static class OrdersController
    {
        // esta constante queda porque el metodo se quito como decia la tarea y asi queda el mensaje guardado
        public const string Message = "This controller does nothing. Endpoints are in Program.cs";
    }
}
